import { StyleSheet } from 'react-native';

export default StyleSheet.create({
    textInput: {
        padding: 6,
        borderWidth: 1,
        borderColor: '#ccc',
        borderRadius: 2,
        backgroundColor: '#fff'
    }
});